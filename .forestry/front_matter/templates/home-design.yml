---
label: Design
hide_body: true
fields:
- name: logo_or_title
  type: field_group
  config: {}
  fields:
  - name: upload_logo
    type: file
    config:
      maxSize: 250
    label: Upload Logo
  - name: title
    type: text
    config:
      required: false
    label: Title
    description: Display text instead of a logo
  label: Logo or Title
- name: main_image
  type: field_group
  config: {}
  fields:
  - name: hero_title
    type: text
    config:
      required: false
    label: Hero Title
    description: The main title overlaying the main image or video
  - name: hero_subtitle
    type: text
    config:
      required: false
    label: Hero Subtitle
    description: the subtitle overlaying the hero image or video
  - name: home_background_image
    type: file
    config:
      maxSize: 250
    label: Home Background Image
    description: Recommended size of 2000 x 1075.
  - name: video_background
    type: boolean
    label: Video Background
    description: Toggle to enable background video instead of an image
  - name: upload_video
    type: file
    config:
      maxSize: 250
    label: Upload Video
  - name: add_url_to_youtube_video
    type: text
    config:
      required: false
    label: Add URL to Youtube Video
  label: Main Image
- name: home_sections
  type: blocks
  label: Home Sections
  template_types:
  - featured-listings
  - sold-listings
  - cta
  - intro
  - featured-listings
  config:
    min: 
    max: 
  description: Move home section areas
pages:
- data/design.yaml
