---
label: Featured Listings
hide_body: true
fields:
- name: address
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: property-listings
  label: Address
